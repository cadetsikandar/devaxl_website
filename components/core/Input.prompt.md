Single-line text field on a dark surface; focus brings up the amber ring.

```jsx
<Input label="Work email" placeholder="you@company.com" hint="We'll never share it." />
```

Props: `label`, `hint`, `prefix`, `invalid`. Standard input attributes (`type`, `value`, `onChange`, `placeholder`) pass through.

Default content surface — a near-black lifted panel with a hairline ring and soft shadow.

```jsx
<Card interactive>
  <h3>Platform engineering</h3>
  <p>Production-grade infra from day one.</p>
</Card>
```

Props: `interactive` (hover lift), `highlight` (amber glow — use on a single hero card only), `padding`. Compose freely with type tokens inside.
